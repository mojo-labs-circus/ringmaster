"""tui/app.py
Phase 3 TUI — not yet built.

Connects to the FastAPI WebSocket, handles JWT auth (tui/auth.py),
streams token/status/done frames, and supports confirm_request interrupts.
See spec/phases.md Phase 3 checklist for full requirements.
"""
